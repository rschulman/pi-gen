# Password database setup
passdb {
    driver = ldap
    args = /data/conf/dovecot/dovecot-ldap.conf.ext
}

# User existence check
userdb {
    driver = ldap
    args = /data/conf/dovecot/dovecot-ldap.conf.ext
}